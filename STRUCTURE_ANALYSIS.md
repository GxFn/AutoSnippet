# 项目文件结构分析 - 问题清单

## 🔴 **严重问题（必须解决）**

### 1. **AutoSnippet/AutoSnippet 目录重复**
```
AutoSnippet/
  ├── AutoSnippet/          ← 重复的嵌套目录
  ├── AutoSnippet.boxspec.json
  └── recipes/
```
**问题**: 目录嵌套不清晰，boxspec 位置不明确

**建议**: 检查是否可以简化为一层结构

---

### 2. **lib/infra 和 lib/infrastructure 重复**
```
lib/
  ├── infra/           (1个文件)
  └── infrastructure/   (11个文件)
```
**问题**: 两个几乎相同的目录名，内容分散

**建议**: 合并到单一目录 `lib/infrastructure/`

---

### 3. **test 和 tests 重复**
```
根目录:
  ├── test/        (unit/, integration/, fixtures/, manual/, temp/)
  └── tests/       (e2e/, benchmarks/, coverage/)
```
**问题**: 命名只差1个's'，容易混淆，测试分散

**建议**: 统一到 `test/` 目录:
```
test/
  ├── unit/
  ├── integration/
  ├── e2e/
  ├── fixtures/
  ├── manual/
  └── README.md
```

---

### 4. **lib/business 和 lib/domain 职责重叠不清**
```
lib/
  ├── business/
  │   ├── metrics/
  │   ├── recipe/
  │   └── search/
  └── domain/
      └── entities/          (几乎为空)
```
**问题**: 边界不清晰，business 应该属于 domain layer

**建议**: 合并到 `lib/domain/` 下

---

## 🟡 **中等问题（建议改进）**

### 5. **docs 和 copilotDocs 职责混淆**
```
根目录:
  ├── docs/           (用户文档)
  └── copilotDocs/    (AI提示、Skills)
```
**问题**: 两个文档目录，用户不知道去哪里找

**建议**: 考虑统一或按类型分类

---

### 6. **recipes 在两个位置**
```
根目录:
  └── recipes/        (4个文件，几乎为空)

AutoSnippet/
  └── recipes/        (实际的recipes库)
```
**问题**: 重复且前者已弃用，增加混淆

**建议**: 删除根目录的 `recipes/`

---

### 7. **bin 混合了不同类型的文件**
```
bin/
  ├── asd                   (可执行文件)
  ├── asd-cli.js           (Node脚本)
  ├── api-server.js        (服务脚本 - 应在 scripts/)
  ├── dashboard-server.js  (服务脚本 - 应在 scripts/)
  └── dashboard/           (子模块)
```
**问题**: 命令行工具和服务脚本混在一起

**建议**:
- `bin/` 只放可执行的命令行工具
- 服务脚本移到 `scripts/`

---

### 8. **scripts 目录职责过于宽泛**
```
scripts/ (28个文件):
  ├── build-*.js
  ├── init-*.js
  ├── install-*.js
  ├── migrate-*.js
  ├── dashboard-server.js  (应在bin/)
  ├── api-server.js        (应在bin/)
  ├── release.js
  ├── verify-*.js
  └── cursor-rules/
```
**问题**: 28个文件在一个目录，难以浏览和理解

**建议**: 分类组织
```
scripts/
  ├── build/          (构建脚本)
  ├── setup/          (初始化、安装脚本)
  ├── migration/      (已存在)
  ├── cursor-rules/   (已存在)
  ├── release.js      (发布脚本)
  └── verify-*.js     (验证脚本)
```

---

### 9. **lib 目录层级复杂且无清晰分层**
```
lib/ (31个子目录):
  ├── agent/
  ├── ai/
  ├── api/
  ├── application/
  ├── automation/
  ├── business/
  ├── candidate/
  ├── cli/
  ├── context/
  ├── core/
  ├── dashboard/
  ├── domain/
  ├── guard/
  ├── infra/
  ├── infrastructure/   (重复)
  ├── injection/
  ├── mcp/
  ├── migration/
  ├── quality/
  ├── recipe/
  ├── search/
  ├── services/
  ├── snippet/
  ├── spm/
  └── watch/
```
**问题**:
- 目录过多（31个）
- DDD和传统分层混合
- 同级目录关系不清

**建议**: 采用清晰的分层
```
lib/
  ├── core/              (基础工具)
  ├── domain/            (业务实体、规则)
  ├── application/       (应用服务)
  ├── infrastructure/    (外部调用)
  └── features/          (特定功能)
      ├── snippet/
      ├── recipe/
      ├── search/
      ├── guard/
      ├── candidate/
      ├── spm/
      └── ai/
```

---

### 10. **命名不统一**
- `copilotDocs` vs `docs` - camelCase vs lowercase
- `infra` vs `infrastructure` - 缩写 vs 全称
- `test` vs `tests` - 单数 vs 复数
- `agent` vs `agents` - 不一致

**建议**: 统一为 **lowercase + plural**:
```
✓ docs
✓ skills
✓ tests
✓ scripts
✓ resources
✓ templates
```

---

## 📊 **优先级排序**

### 🔴 必须修复
1. **test 和 tests** - 合并到 `test/`
2. **lib/infra 和 lib/infrastructure** - 合并为 `lib/infrastructure/`
3. **根目录 recipes/** - 删除(已弃用)

### 🟡 强烈建议
4. **lib 重构** - 采用清晰的分层架构
5. **scripts 分类** - 按功能分目录

### 🟢 可选改进
6. **bin 清理** - 分离服务脚本
7. **命名统一** - camelCase → lowercase

---

## 📈 **改进后的项目结构参考**

```
AutoSnippet/
├── .git/
├── .github/
├── .editorconfig
├── .prettierrc
├── AutoSnippet.boxspec.json
├── package.json
│
├── bin/                      ← 命令行工具
│   ├── asd
│   ├── asd-verify
│   └── dashboard/
│
├── lib/                      ← 核心代码库（清晰分层）
│   ├── core/                 (常量、工具)
│   ├── domain/               (实体、规则)
│   ├── application/          (应用服务)
│   ├── infrastructure/       (外部调用)
│   └── features/
│       ├── ai/
│       ├── candidate/
│       ├── recipe/
│       ├── search/
│       ├── snippet/
│       └── spm/
│
├── dashboard/                ← 前端应用
│
├── AutoSnippet/              ← 知识库
│   ├── recipes/
│   ├── .autosnippet/
│   └── AutoSnippet.boxspec.json
│
├── skills/                   ← MCP Skills文档
│
├── resources/                ← 资源文件
│
├── scripts/                  ← 开发脚本（按类型分类）
│   ├── build/
│   ├── setup/
│   ├── migration/
│   ├── cursor-rules/
│   └── release.js
│
├── test/                     ← 统一的测试根目录
│   ├── unit/
│   ├── integration/
│   ├── e2e/
│   ├── fixtures/
│   ├── manual/
│   └── README.md
│
├── docs/                     ← 所有文档
│   ├── guides/
│   ├── api/
│   └── templates/
│
├── config/
└── tools/
```

---

## ✅ **快速行动清单**

- [ ] 删除或合并 `test/` 和 `tests/`
- [ ] 合并 `lib/infra/` 到 `lib/infrastructure/`
- [ ] 删除根目录的 `recipes/` 目录
- [ ] 规划 `lib/` 重构（新的分层结构）
- [ ] 组织 `scripts/` 为子目录
- [ ] 统一命名规范为 lowercase + plural
