# ──────────────────────────────────────────────────────
# Alembic V2 CI Pipeline
# 构建 → API 验证 → 单元测试 → 集成测试
# ──────────────────────────────────────────────────────
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  API_BASE: http://localhost:3100/api/v1
  NODE_ENV: test

jobs:
  build:
    name: Build & Lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['22']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - run: npm run lint
      - run: cd dashboard && npm ci
      - run: npm run build:dashboard

  api-smoke:
    name: API Smoke Test
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - run: cd dashboard && npm ci
      - run: npm run build:dashboard

      - name: Start API Server
        run: |
          ASD_SKIP_ENTRY_CHECK=1 ASD_DISABLE_WRITE_GUARD=1 ASD_DISABLE_RATE_LIMIT=1 \
            npm run dashboard -- --port 3100 --no-open > /tmp/api-server.log 2>&1 &
          echo "PID=$!" >> $GITHUB_ENV
          sleep 2

      - name: Wait for readiness
        run: |
          for i in $(seq 1 30); do
            if curl -sf $API_BASE/health > /dev/null 2>&1; then
              echo "✓ API ready (${i}s)"
              exit 0
            fi
            sleep 1
          done
          echo "✗ API not ready after 30s"
          cat /tmp/api-server.log
          exit 1

      - name: Health check
        run: |
          HEALTH=$(curl -sf $API_BASE/health)
          echo "$HEALTH" | jq .
          echo "$HEALTH" | jq -e '.status == "healthy"'

      - name: Readiness check
        run: |
          curl -sf $API_BASE/health/ready | jq -e '.ready == true'

      - name: Root endpoint
        run: |
          ROOT=$(curl -sf $API_BASE/../..)
          echo "$ROOT" | jq .
          echo "$ROOT" | jq -e '.name == "Alembic API"'

      - name: Knowledge listing
        run: |
          curl -sf $API_BASE/knowledge | jq -e '.success == true'

      - name: Upload server log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: api-server-log
          path: /tmp/api-server.log

  unit-tests:
    name: Unit Tests
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - run: npm run test:unit

  integration-tests:
    name: Integration Tests
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run build

      - name: Show Node/npm version
        run: node -v && npm -v

      - name: Run integration tests
        run: npm run test:integration 2>&1 | tee /tmp/integration-test.log
        env:
          ASD_DISABLE_WRITE_GUARD: '1'
          ASD_DISABLE_RATE_LIMIT: '1'

      - name: Upload test log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-log
          path: /tmp/integration-test.log