{
  "name": "asd",
  "displayName": "Alembic",
  "description": "Knowledge-driven code snippets — search, insert, create, and audit directly in your editor",
  "version": "0.1.0",
  "publisher": "asd",
  "license": "MIT",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Snippets",
    "Other"
  ],
  "keywords": [
    "snippet",
    "knowledge",
    "code-search",
    "asd"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "asd.search",
        "title": "Alembic: Search Knowledge Base",
        "icon": "$(search)"
      },
      {
        "command": "asd.create",
        "title": "Alembic: Create Candidate from Selection"
      },
      {
        "command": "asd.audit",
        "title": "Alembic: Audit Current File"
      },
      {
        "command": "asd.auditProject",
        "title": "Alembic: Audit Entire Project"
      },
      {
        "command": "asd.status",
        "title": "Alembic: Show Connection Status"
      },
      {
        "command": "asd.toggleRemotePoller",
        "title": "Alembic: Toggle Remote Command Poller"
      },
      {
        "command": "asd.startRemotePoller",
        "title": "Alembic: Start Remote Command Poller"
      },
      {
        "command": "asd.stopRemotePoller",
        "title": "Alembic: Stop Remote Command Poller"
      }
    ],
    "configuration": {
      "title": "Alembic",
      "properties": {
        "asd.serverPort": {
          "type": "number",
          "default": 3000,
          "description": "Port of the Alembic API server (asd ui / asd start)"
        },
        "asd.serverHost": {
          "type": "string",
          "default": "localhost",
          "description": "Host of the Alembic API server"
        },
        "asd.enableDirectiveDetection": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic detection of Alembic directives in code"
        },
        "asd.enableCodeLens": {
          "type": "boolean",
          "default": true,
          "description": "Show CodeLens actions above Alembic directives"
        },
        "asd.enableGuardDiagnostics": {
          "type": "boolean",
          "default": true,
          "description": "Enable Guard diagnostics on file save (shows violations as editor warnings)"
        },
        "asd.insertHighlightDuration": {
          "type": "number",
          "default": 2000,
          "description": "Duration (ms) to highlight inserted code"
        },
        "asd.enableRemotePoller": {
          "type": "boolean",
          "default": false,
          "description": "Enable remote command polling (飞书/Lark Bot → IDE bridge). When enabled, the extension polls for remote commands and injects them into Copilot Chat."
        }
      }
    },
    "keybindings": [
      {
        "command": "asd.search",
        "key": "ctrl+shift+f5",
        "mac": "cmd+shift+f5"
      }
    ],
    "languageModelTools": [
      {
        "name": "asd",
        "displayName": "Alembic",
        "toolReferenceName": "asd",
        "canBeReferencedInPrompt": true,
        "modelDescription": "Project persistent memory. Stores team decisions and tracks tasks across sessions.\n\nWorkflow: 1) prime → load decisions & tasks. 2) Respect ALL returned decisions. 3) record_decision when user agrees/disagrees on something. 4) create/close to track work.\n\nKey operations: prime (load memory), record_decision (save agreement), create (new task), close (finish task), list/show/stats (query).",
        "icon": "$(database)",
        "inputSchema": {
          "type": "object",
          "properties": {
            "operation": {
              "type": "string",
              "description": "Operation: prime, record_decision, revise_decision, unpin_decision, create, close, list, show, stats, ready, claim, progress, fail, defer, decompose, blocked, dep_add, dep_tree"
            }
          },
          "required": ["operation"]
        }
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./tsconfig.json",
    "watch": "tsc -watch -p ./tsconfig.json",
    "package": "vsce package",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/vscode": "^1.109.0",
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3",
    "@vscode/vsce": "^3.7.1"
  }
}
